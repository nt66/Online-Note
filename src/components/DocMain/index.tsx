import { useState } from 'react'
import './index.less'

function DocMain() {
  return (
    <div className='doc-container' style={{marginLeft: '343px', overflow: 'hidden', zIndex: 'auto'}}>
      <div className='doc-wrap'>

      </div>
    </div>
  )
}

export default DocMain
